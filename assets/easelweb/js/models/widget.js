/**
    Easelweb Widget Model - 
 */
define([
    'backbonePkg',
],
function (Backbone) {
    // Easelweb Widget Model
    var exports = Backbone.Model.extend({
        /**
         * Initialization
         * @param {object} options The view options
         */
        initialize: function(options) {
            
        },
    });

    // Export module
    return exports;
});